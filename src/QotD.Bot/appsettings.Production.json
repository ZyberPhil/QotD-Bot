{
    "Discord": {
        "Token": "${DISCORD_TOKEN}",
        "GuildId": "${DISCORD_GUILD_ID}",
        "ChannelId": "${DISCORD_CHANNEL_ID}"
    },
    "ConnectionStrings": {
        "Postgres": "Host=postgres;Port=5432;Database=qotd;Username=qotd;Password=${POSTGRES_PASSWORD};Pooling=true;Minimum Pool Size=5;Maximum Pool Size=20;Connection Idle Lifetime=300;Connection Lifetime=600"
    },
    "Serilog": {
        "MinimumLevel": {
            "Default": "Information"
        }
    }
}