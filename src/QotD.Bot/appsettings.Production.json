{
    "Discord": {
        "Token": "${DISCORD_TOKEN}",
        "GuildId": "${DISCORD_GUILD_ID}",
        "ChannelId": "${DISCORD_CHANNEL_ID}"
    },
    "ConnectionStrings": {
        "Postgres": "Host=postgres;Port=5432;Database=qotd;Username=qotd;Password=${POSTGRES_PASSWORD}",
        "LevelingSqlite": "Data Source=/app/data/leveling.db"
    },
    "Serilog": {
        "MinimumLevel": {
            "Default": "Information"
        }
    }
}